/*
    This file specifies the properties for the properties
    for the container. Here you can specify the display name
    in the system bar, the default app that's loaded, components
    for the container, and properties for apps.

    For more information, see the page "Container Configuration" 
    in the Container documentation.
*/
define({
    name: '',
    defaultApp: '',
    components: [],
    properties: {}
});
